# Neon PostgreSQL connection string. Keep the real value out of git.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# URL where the backend API is deployed.
VITE_BACKEND_URL=https://your-api.example.com

# Local API port (optional; defaults to 8788).
API_PORT=8788
